name: Manual Test

on:
  push:
    branches:
      - test

jobs:
  test:
    name: Test
    runs-on: [ ubuntu-latest ]
    steps:
      - uses: actions/checkout@v4

      - id: aws_credentials
        uses: ./
        with:
          export: true
          source: "input"
          aws_access_key_id: "qwe"
          aws_secret_access_key: "asd"
          aws_session_token: "zxc"
